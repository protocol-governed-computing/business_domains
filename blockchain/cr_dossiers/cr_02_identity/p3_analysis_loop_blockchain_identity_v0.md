# Stage 3 — Analysis Loop: blockchain / identity

**Stage:** 3 — Analysis Loop
**CR:** cr_02_identity
**Status:** DRAFT
**Feeds:** Stage 4 — Business Model

Each gap and concern carried from Stage 2 is driven to a committed decision against the pinned
composition. This change adds a way in to a function that already runs, so every question here is
about the boundary and none is about identity's behaviour, which is unchanged.

---

## 1. Analysis Findings

<!-- register:analysis_findings -->
| Question Id | Finding | Impact | Evidence Status (OBSERVED, INFERRED, OPEN) | Confidence (HIGH, MEDIUM, LOW) | Resolution Status (CLOSED, OPEN) | Evidence |
|-------------|---------|--------|-----------------|------------|-------------------|----------|
| Q1 | The business asked for one way in with the act named in the request, and that shape is already proven in the composition at seventeen acts. The inspection namespace binds seventeen acts to a single place, the act being named in what the caller sends; the reference workload binds its one act to a place of its own. Both are supported by the same boundary, and which is used is a property of how names are bound, not of any artifact this change authors. | The two identity acts share one way in. Adding the remaining five functions later adds names, not places, which is what the business asked for when it declined to hand callers a new address for every act. | OBSERVED | HIGH | CLOSED | The composition declares eighteen transport ingress and eighteen transport egress artifacts; seventeen ingress artifacts of the inspection namespace share one way in, and workload::TI_COLLATZ_COMPUTE_V0 does not |
| Q2 | A public name and the act it reaches are separate declarations, which is what allows the name to outlive the act. workload::TI_COLLATZ_COMPUTE_V0 carries the public name, and separately the workflow that name is bound to, stating in terms that the bound workflow is an implementation detail that may be re-pointed. | The two names this change introduces are the business's own words for its acts, and neither is a workflow's name. Re-pointing either act later changes no caller. | OBSERVED | HIGH | CLOSED | workload::TI_COLLATZ_COMPUTE_V0 declares an operation identity and a separately declared handler naming workload::WF_COLLATZ_CONJECTURE_V0 |
| Q3 | Both identity acts are handed their own configuration through the same door as the caller's data. Admitting a person requires the declaration the registration is read against, where the address sits, what kind it is, the stream the occurrence belongs to and the occurrence's own fields; recording a decision additionally requires the states that admit a decision and the outcomes permitted. Only the person's details and the authority's decision belong to a caller. | A caller cannot be asked for any of it, because supplying the declaration a registration is read against is supplying the test. Something between the caller and the act must hold it. | OBSERVED | HIGH | CLOSED | blockchain::WF_REGISTER_ACTOR_V0 draws actor_record, registration_schema, address_path, address_type, stream_id and occurrence_fields from its payload; blockchain::WF_RECORD_VERIFICATION_DECISION_V0 additionally draws states_admitting_a_decision, admitted_outcomes and decided_actor_fields |
| Q4 | The ingress declaration holds that configuration. Its template maps what a caller sends onto what the act needs, substituting only the caller's own values and passing every other value through as written, including whole nested structures the caller never sends. The reference workload already relies on this, wrapping a caller's single number in a list the caller does not supply. | The rules an act needs but a caller must not send are declared in a compiled artifact sealed into the snapshot. They are held once, where a change to them is a governed change, and never in the web page. | OBSERVED | HIGH | CLOSED | workload::TI_COLLATZ_COMPUTE_V0 declares a payload template substituting the caller's value into a literal list structure; the boundary substitutes only declared input keys and fails rather than forwarding an undeclared one |
| Q5 | Holding the configuration at the boundary is the right decision for this change and the wrong place for it in the long run. The alternative — the workflows drawing their configuration from a declared structure of their own — is better placement, because the rules would sit with the function that owns them rather than with the door it is reached through. It reopens two sealed workflows, which this change states it does not do. | The configuration is held in the ingress declarations now, and moving it into the domain is recorded as a change of its own. Both acts keep working unchanged in the meantime. | OBSERVED | HIGH | CLOSED | S1 out_of_scope #3 states this change revisits no decision the previous change made; blockchain::WF_REGISTER_ACTOR_V0 and blockchain::WF_RECORD_VERIFICATION_DECISION_V0 would both require amendment under the alternative |
| Q6 | The readability test the business requires is already the reusable form, and reaching it from outside requires a second statement of it rather than a second test. The act's own contract takes both the record and the declaration it is read against as inputs, so the declaration held at the boundary is the same declaration the act applies. What the boundary states about a caller's request must agree with it. | One test, stated at the boundary as what a caller may send and applied by the act as the declaration it validates against. The business's refusal to have two tests is honoured by both statements naming the same fields. | OBSERVED | HIGH | CLOSED | blockchain::CC_VALIDATE_REGISTRATION_V0 runs capability_transforms::CT_PURE_VALIDATE_RECORD_STRUCTURE_V0 over a record and a schema, both supplied as inputs |
| Q7 | Three of the four refusals the business names are already distinguishable at the boundary. An act the business does not offer is refused by the boundary before any act is reached and carries its own class. An actor that does not exist is answered by the act and carries the class for a subject that is absent. A request whose details cannot be read is refused at the door. The classes are a closed governed set of six. | Only one pair collides, and the other refusals need nothing authored to keep them apart. | OBSERVED | HIGH | CLOSED | fb.transport::CONSTITUTION_TRANSPORT_EGRESS_V0 declares the closed set SUCCESS, VIOLATION, UNAUTHORIZED, NOT_FOUND, OPERATION_NOT_FOUND and EXECUTION_FAILURE, and requires that a subject absent and an act not offered are never conflated; executing blockchain::WF_RECORD_VERIFICATION_DECISION_V0 against an unregistered actor returns NOT_FOUND |
| Q8 | The remaining pair collides in the kind of the answer and differs in its detail. A request whose details cannot be read and an act that ran and refused both carry the class for a violation. What the caller receives is not identical: the first names the fields at fault, the second reports the act's own status. The business requires that the two are never reported as the same answer, which two answers differing in what they state satisfy; it does not require a distinct kind, and the governed set has no kind to give. | The requirement is met by what the answer states. That the two share a kind is recorded as a concern, and a class for an act that ran and refused is named as a change to the platform's governed set, which this change does not make. | OBSERVED | MEDIUM | CLOSED | The boundary refuses an unreadable request with the offending fields named, and reports an act's refusal as its terminal status; executing blockchain::WF_RECORD_VERIFICATION_DECISION_V0 against an actor already decided about returns VIOLATION, as does a registration missing its address |
| Q9 | An act that refuses says that it refused and not what it refused for. Every refusal in both acts — a registration that cannot be read, an address that could not be claimed, an actor that does not exist, an actor already decided about, an authority deciding about itself — reaches a single ending. A caller told the business refused cannot be told which refusal it was. | Within this change the caller is told that the act refused. Telling them which refusal requires the acts to distinguish their endings, which is an amendment to sealed workflows and belongs with the placement change recorded in Q5. | OBSERVED | HIGH | CLOSED | Both workflows route every refusing outcome to one ending; executing the deciding act against a self-verifying authority returns NOT_FOUND rather than a refusal naming self-verification |
| Q10 | The events this business named are declared and cannot fire. Three event artifacts exist for the moments an actor is registered, accepted and rejected, and neither act reaches them: the ending that would announce a moment carries a field the platform does not read, and does not carry the field it does. Every domain authored through the pipeline shows this and every hand-authored domain does not. | Nothing in this change depends on an event firing — the caller is answered from the act's own result, and the trail of occurrences is written by the acts themselves. The defect is in construction rather than in either domain, and is named as a change against the pipeline. | OBSERVED | HIGH | CLOSED | blockchain declares three event artifacts and book_library_mgmt six, and no workflow of either domain announces one; ai_governance and workload announce theirs from the ending reached; a trace of blockchain::WF_REGISTER_ACTOR_V0 records the act's steps and no event |
| Q11 | The front page and the two forms are not artifacts of the composition and are not governed by it. What the business offers is declared in the composition; what a page shows about it is not. Marking four functions as not yet available is a statement the pages make, which nothing in the composition confirms or contradicts. | The pages are built and are held to the business's constraints — no rule, no check before sending, carrying only what the person typed — by the acceptance criteria rather than by a compiled artifact. | OBSERVED | HIGH | CLOSED | No artifact of the composition declares a web page; S1 acceptance_criteria #11 and #14 state the tests that hold the page thin |

## 2. Verification Results

<!-- register:verification_results -->
| Item | Origin | Result (CONFIRMED, OVERTURNED) | Evidence |
|------|--------|--------------------------------|----------|
| The business author believes the identity function already exists, with both acts working. | S2 belief_verification #1 | CONFIRMED | Seventeen artifacts in the blockchain namespace, all owned by identity; executing blockchain::WF_REGISTER_ACTOR_V0 against the pinned snapshot returns SUCCESS and writes an occurrence |
| The business author believes the platform already offers a way to be reached from outside, and that identity does not have one yet. | S2 belief_verification #2 | CONFIRMED | Eighteen ingress and eighteen egress artifacts, none in the blockchain namespace |
| The business author believes the readability test defined by the previous change can be reused as it stands, rather than restated. | S2 belief_verification #3 | CONFIRMED | blockchain::CC_VALIDATE_REGISTRATION_V0 takes the record and the declaration as inputs and applies capability_transforms::CT_PURE_VALIDATE_RECORD_STRUCTURE_V0 |
| The business author believes the platform can already distinguish a request it declines to begin from an act that ran and decided against the caller. | S2 belief_verification #4 | CONFIRMED | Resolved in Q7 and Q8: three of the four refusals carry distinct governed classes, and the remaining pair differs in what the answer states while sharing its kind |
| The business author believes the other five blockchain functions have nothing to offer from outside yet. | S2 belief_verification #5 | CONFIRMED | Every blockchain artifact declares identity as its owning subdomain |
| Both acts declare their successful ending to be a refusal | S2 discovery_concerns #1 | CONFIRMED | Resolved in Q10 and shown to be inert: the field is read by nothing, and the same ending fails to carry the field that is read |
| The declaration a registration is read against is supplied by its caller | S2 discovery_concerns #2 | CONFIRMED | Resolved in Q3 and Q4: the declaration is held in the ingress artifact, so one caller supplies it and it is sealed |
| An act that refuses does not say what it refused for | S2 discovery_concerns #3 | CONFIRMED | Resolved in Q9 and carried forward with the placement change in Q5 |
| The five unbuilt functions are absent rather than declared absent | S2 discovery_concerns #4 | CONFIRMED | Resolved in Q11: what a page shows is not governed by the composition |

## 3. Dependency Discoveries

<!-- register:dependency_discoveries -->
| Dependency | Type | Disposition (EXISTING, REUSE, AUTHOR_NEW, INVESTIGATE) | Evidence |
|------------|------|------------------------|----------|
| The boundary's ingress and egress kinds | Governed artifact kind | EXISTING | fb.transport::CONSTITUTION_TRANSPORT_INGRESS_V0 and fb.transport::CONSTITUTION_TRANSPORT_EGRESS_V0 govern them, and thirty-six compiled artifacts of those kinds are sealed in the composition |
| The act that admits a person | Workflow | REUSE | blockchain::WF_REGISTER_ACTOR_V0, unchanged; impacted by nothing in the composition |
| The act that records a decision | Workflow | REUSE | blockchain::WF_RECORD_VERIFICATION_DECISION_V0, unchanged; impacted by nothing in the composition |
| Reading a record for absence and form | Capability transform | REUSE | capability_transforms::CT_PURE_VALIDATE_RECORD_STRUCTURE_V0, impacted by 31 artifacts, reached through blockchain::CC_VALIDATE_REGISTRATION_V0 |
| The name a caller uses for an act | Governed declaration | AUTHOR_NEW | No artifact of the blockchain namespace declares a public name for either act |
| What a caller may send, and what the act is handed | Governed declaration | AUTHOR_NEW | The same; the configuration held here is drawn from the acts' own declared payloads |
| The kinds of answer a caller is told | Governed declaration | AUTHOR_NEW | The same; the closed set of classes is governed and is not extended by this change |
| A source layer for the boundary declarations | Build manifest | AUTHOR_NEW | blockchain::STRUCTURE_BUILD_BLOCKCHAIN_CONFIG_V0 already admits both kinds among its artifact types and declares one source layer; the workload manifest declares a separate transport layer and a second identity rule alongside its registry layer |
| The table binding a place to a name | Adapter configuration | REUSE | The binding table is data the adapter is pointed at, not an artifact of the composition; the inspection surface and the reference workload each carry one |
| The web pages | Client | AUTHOR_NEW | No artifact of the composition declares one; the legacy pages of the prior implementation are lifted one way and depend on nothing in it |
| Announcing the moments an actor is registered, accepted and rejected | Domain event | INVESTIGATE | Three event artifacts exist and cannot fire; the cause is in construction and is carried as a change of its own, on which nothing in this one depends |
| Establishing who a caller is | Governed declaration | INVESTIGATE | Deferred by the business with the question of who may be an authority; nothing in the composition establishes a caller's identity |

## 4. Impact Analysis

<!-- register:impact_analysis -->
| Artifact | Impact Scope | Consumer Count | Evidence |
|----------|--------------|----------------|----------|
| blockchain::WF_REGISTER_ACTOR_V0 | Named by a new boundary declaration; not amended | 0 | si.topology.impact impacted_count 0 |
| blockchain::WF_RECORD_VERIFICATION_DECISION_V0 | Named by a new boundary declaration; not amended | 0 | si.topology.impact impacted_count 0 |
| blockchain::CC_VALIDATE_REGISTRATION_V0 | Reached unchanged through the act; the declaration it validates against is now held at the boundary | 2 | si.topology.impact impacted_count 2 |
| blockchain::AC_PARTICIPANT_V0 | Reused unchanged; a caller from outside is not established as this or any actor | 0 | si.topology.impact impacted_count 0 |
| capability_transforms::CT_PURE_VALIDATE_RECORD_STRUCTURE_V0 | Reused unchanged | 31 | si.topology.impact impacted_count 31 |
| workload::TI_COLLATZ_COMPUTE_V0 | Not changed; read as the precedent for a public name, a caller's declared input and a template holding what a caller does not send | 0 | si.topology.impact impacted_count 0 |
| workload::TE_COLLATZ_COMPUTE_V0 | Not changed; read as the precedent for classifying an act's outcome and exposing evidence by reference | 0 | si.topology.impact impacted_count 0 |
| fb.transport::CONSTITUTION_TRANSPORT_INGRESS_V0 | Governs the new declarations; not amended | 0 | si.topology.impact impacted_count 0 |
| fb.transport::CONSTITUTION_TRANSPORT_EGRESS_V0 | Governs the new declarations; not amended, and its closed set of classes is not extended | 0 | si.topology.impact impacted_count 0 |
| blockchain::STRUCTURE_BUILD_BLOCKCHAIN_CONFIG_V0 | Amended to declare where the boundary declarations are found | 0 | It already admits both kinds among its artifact types while declaring no layer they could be found in |

## 5. Authoring Decisions

<!-- register:authoring_decisions business_language=capability -->
| Capability | Decision (REUSE, EXTEND, AUTHOR_NEW) | Rationale | Alternatives Checked | Source Finding |
|------------|----------|-----------|----------------------|----------------|
| Admit a person's registration | REUSE | The act exists, runs and is unchanged by this change. Only the way in is new. | Amending it to take its configuration from a declared structure of its own was checked and deferred in Q5: it is better placement and it reopens a sealed act. | S3 analysis_findings Q5 |
| Record an authority's decision | REUSE | The same. | The same. | S3 analysis_findings Q5 |
| Read a registration for absent or malformed details | REUSE | The business refuses to have two tests, and the one it has takes the declaration it applies as an input, so it is already the reusable form. | Restating the test at the boundary as a second test was checked and rejected: two tests for one question is how they come to disagree. | S3 analysis_findings Q6 |
| Offer registering an actor to a caller outside the business | AUTHOR_NEW | The act has no public name, so nothing a caller could ask for reaches it. The name is the business's own word for the act and is declared apart from the act it reaches. | Naming the act by the workflow that performs it was checked and rejected: it would bind every caller to an implementation the business intends to be free to change. | S3 analysis_findings Q2 |
| Offer recording a verification decision to a caller outside the business | AUTHOR_NEW | The second act, needing its own name because what a caller may send differs entirely from registration. | Sharing one name with registration was checked and rejected: the two carry different required values and would need the caller to say which they meant. | S3 analysis_findings Q2 |
| Hold what an act requires and a caller must not send | AUTHOR_NEW | The acts are handed their own rules through the same door as the caller's data. Held in the declaration that admits the request, they are sealed in the snapshot and stated once. | Asking the caller for them was checked and rejected — it is supplying the rules that judge you. Holding them in the web page was checked and rejected by the business outright. | S3 analysis_findings Q4 |
| Tell a caller how their registration ended | AUTHOR_NEW | The kinds of answer are governed and closed, and which of them each ending takes is declared per act. | Leaving the act's result to be shown as it stands was checked and rejected: it exposes what the act holds rather than what the business exposes. | S3 analysis_findings Q7 |
| Tell a caller how their decision ended | AUTHOR_NEW | The same, over the endings that act reaches, including the absent actor which is already a distinct kind. | Sharing one statement of answers with registration was checked and rejected: the two acts end differently. | S3 analysis_findings Q7 |
| Offer both acts through one way in | REUSE | The business asked for one way in with the act named in the request, and seventeen acts already share one. What binds a place to a name is configuration the boundary is pointed at, not an artifact. | A place for each act was checked and rejected against the business's statement that it will add many acts and will not hand callers a new address for each. | S3 analysis_findings Q1 |
| Declare where the boundary declarations are found | EXTEND | The domain's build manifest already admits both kinds while declaring no place they could be found. The worked precedent declares a second source layer beside the registry one. | Placing them among the domain's existing artifacts was checked; the precedent keeps them apart, and keeping them apart makes what is reachable from outside legible at a glance. | S3 dependency_discoveries #8 |
| Show a person a form and its answer | AUTHOR_NEW | Two forms and a front page. The pages are lifted from the prior implementation, which harvests what is typed and renders what comes back without knowing any schema. | Building them new was checked and rejected: the existing pages already hold no rules, which is the constraint the business cares about. What they say to the platform is replaced, because they name an act's implementation and read an envelope this platform does not produce. | S3 analysis_findings Q11 |
| Carry a detail from one page to the next | AUTHOR_NEW | A convenience the business asked for, holding only what the person just typed. | Holding nothing was checked; the business chose the convenience and bounded it so that nothing the business does depends on it. | S3 analysis_findings Q11 |
| Show the functions the business has not built | AUTHOR_NEW | Four of six functions are named and marked not yet available, so a caller sees what is coming instead of something that fails. | Omitting them was checked and rejected against the business's statement of what the front page shows. | S3 analysis_findings Q11 |
| Announce the moments an actor is registered, accepted and rejected | REUSE | The three moments are already declared. That they cannot presently be announced is a defect in how artifacts are constructed, not in this domain, and nothing in this change depends on one being announced. | Redeclaring them here was checked and rejected: they exist and are correct. Repairing construction was checked and placed outside this change, which authors no pipeline. | S3 analysis_findings Q10 |

## 6. Placement Decision

<!-- register:placement_decision business_language=rationale -->
| Decision (NEW_SUBDOMAIN, EXTEND) | Subdomain | Rationale | Source Finding |
|----------|-----------|-----------|----------------|
| EXTEND | identity | The acts being offered are identity's own, and what is added says only how they are reached. A subdomain of its own would separate the name of an act from the act it names, leaving each half owned by a different part of the business. Every artifact of this domain already declares identity as its owner. | S3 analysis_findings Q2 |

## 7. Saturation Assessment

<!-- register:saturation business_language=criterion -->
| Criterion | Status (SATISFIED, NOT_SATISFIED) | Evidence |
|-----------|--------|----------|
| No unresolved CRITICAL gaps | SATISFIED | The four CRITICAL gaps carried from Stage 2 each resolve to a committed decision: the missing public names and the missing statement of what a caller may send to AUTHOR_NEW in Q2, the configuration travelling as payload to the boundary declarations in Q4, and the colliding refusals to the reading settled in Q8 with the residual concern named |
| No open analyst questions | SATISFIED | All eleven findings in this stage are CLOSED. The one business question this stage raised was put to the business author and answered before it ran: the configuration is held at the boundary rather than moved into the domain |
| No dependency expansion in the last pass | SATISFIED | The twelve dependencies were established in one pass against the composition's transport surface, the domain's own artifacts and its build manifest; re-reading them surfaced only the manifest's missing source layer, which was already among them |
| Verification pass complete, no OVERTURNED item unresolved | SATISFIED | All nine items re-grounded and CONFIRMED, including the belief returned from Stage 2 as INSUFFICIENT_EVIDENCE, which Q7 and Q8 resolve, and all four Stage 2 discovery concerns |
| Every INFERRED finding promoted to OBSERVED, explicitly accepted, or carried forward with a reason | SATISFIED | Stage 2 carried no INFERRED row. Every finding in this stage is OBSERVED; Q8 alone is held at MEDIUM confidence, because it rests on a reading of what the business requires rather than on a governed class that expresses it |

---

## gov_projection — Governed Handoff to Stage 4

| Direction | Fields |
|-----------|--------|
| **Consumes** ← Stage 1 | cr_type · assumptions · business_invariants · lifecycle_states · business_events · authority_boundaries · out_of_scope · constraints |
| **Consumes** ← Stage 2 | belief_verification · pps_baseline_fqdns · gaps · architectural_observations · discovery_concerns · open_questions |
| **Emits** → Stage 4 | authoring_decisions · dependency_discoveries · placement_decision · saturation |
